/**
 * Placeholder until the JMC-style overview lands (milestone U5 of
 * docs/UI-redesign.md): key JVM/GC/system facts plus CPU, heap, process
 * memory and GC-pause charts.
 */
export function OverviewView() {
  return (
    <section class="view-overview" aria-label="Overview view">
      <p class="empty">The overview charts are not built yet.</p>
    </section>
  );
}
